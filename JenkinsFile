pipeline {
  agent {
    node {
      label 'maven'
    }
  }
  
  stages {
    stage('Test') {
      steps {
        Echo 'Hello Test'
      }
    }
    stage('Package') {
      steps {
        #sh './mvnw clean package -DskipTests -Dquarkus.package.type=uber.jar'
        Echo 'Hello Package'
      }
    }
  }
}
