pipeline {
  agent {
    node {
     label: 'Maven'
    }
  }
  stages {
    stage('Test') {
      steps {
        echo 'hello'
      }
    }
    stage('Package') {
      steps {
        echo 'package ok'
      }
    }
  }
}
