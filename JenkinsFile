pipeline {
  agent {
    node {
      label 'maven'
    }
  }
  
  stages {
    stage('Test') {
      steps {
        echo 'Hello, Hi.'
      }
    }
    stage('Package') {
      steps {
        sh './mvnw package -DskipTests'
        archiveArtifacts 'target/*.jar'
      }
    }
  }
}
